spring:
# database 설정이 없어도 springboot 가 memory mode 로 동작하게 해준다!
#  datasource:
#    url: jdbc:h2:mem:test # memory mode
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # default
#    properties:
#      hibernate:
##        show_sql: true # system out
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # logger
    org.hibernate.orm.jdbc.bind: trace # sqp parameter